
const SID="1TSpt_DxEDhpsXE09lNx8S63b7cDomEXhVua--p99DGM";
function clean(v){return String(v??"").trim()}
function parseCSV(text){const rows=[];let row=[],field="",q=false;for(let i=0;i<text.length;i++){const c=text[i];if(q){if(c=='"'&&text[i+1]=='"'){field+='"';i++}else if(c=='"')q=false;else field+=c}else if(c=='"')q=true;else if(c==','){row.push(field);field=""}else if(c=='\n'){row.push(field);rows.push(row);row=[];field=""}else if(c!='\r')field+=c}if(field||row.length){row.push(field);rows.push(row)}return rows}
async function sheet(tab,header){const u=`https://docs.google.com/spreadsheets/d/${SID}/gviz/tq?tqx=out:csv&sheet=${encodeURIComponent(tab)}&headers=0`;const c=new AbortController(),tm=setTimeout(()=>c.abort(),10000);try{const r=await fetch(u,{signal:c.signal,cache:"no-store"});if(!r.ok)throw new Error(`${tab} HTTP ${r.status}`);const raw=await r.text();if(/<html|accounts\.google\.com|sign in/i.test(raw))throw new Error(`${tab} is not anonymously readable`);const rows=parseCSV(raw),hi=rows.findIndex(r=>clean(r[0]).toLowerCase()===header.toLowerCase());if(hi<0)throw new Error(`${tab} header missing`);const heads=rows[hi].map(clean);return rows.slice(hi+1).filter(r=>r.some(x=>clean(x))).map((r,n)=>({...Object.fromEntries(heads.map((h,i)=>[h,clean(r[i])])),_row:hi+n+2}))}finally{clearTimeout(tm)}}
function json(body,status=200){return Response.json(body,{status,headers:{"cache-control":"no-store"}})}

export default async req=>{if(req.method!=="GET")return new Response("Method not allowed",{status:405});try{const rows=await sheet("Partnership Config","Key");const row=rows.find(r=>clean(r.Key)==="PARTNERSHIP_INFO_URL");const dest=clean(row?.Value);if(!(dest.startsWith("/")||/^https:\/\//i.test(dest)))return new Response("Partnership guide is not configured.",{status:503});const loc=dest.startsWith("/")?new URL(dest,req.url).href:dest;return new Response(null,{status:302,headers:{Location:loc,"cache-control":"no-store"}})}catch(e){return new Response("Partnership guide is temporarily unavailable.",{status:502,headers:{"cache-control":"no-store"}})}};
